% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variant_overlay_snv.R
\name{build_snv_layer}
\alias{build_snv_layer}
\title{Build ggplot2 layers for SNV marks on reads}
\usage{
build_snv_layer(variant_bases)
}
\arguments{
\item{variant_bases}{A data.frame with columns \code{read_name}, \code{position},
\code{lane}, and \code{variant_class} (values: \code{"ref"}, \code{"alt"}, \code{"other"},
\code{"del"}), as returned by \code{\link[=extract_variant_bases]{extract_variant_bases()}}. May be \code{NULL} or
zero-row.}
}
\value{
A list with one ggplot2 \code{geom_point} layer that can be appended to a
ggplot with \code{+}, or \code{NULL} when there are no non-ref calls to draw. All
variant classes are drawn as a small red asterisk (shape 8).
}
\description{
Takes pre-extracted per-read base calls (from \code{\link[=extract_variant_bases]{extract_variant_bases()}}) and
returns a list of ggplot2 layer objects that draw shape-encoded points at the
position/lane of each non-ref call. The layers can be added directly to a
ggplot with \code{+}.
}
\keyword{internal}
