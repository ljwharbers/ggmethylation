% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variant_overlay.R
\name{extract_variant_bases}
\alias{extract_variant_bases}
\title{Extract per-read bases at variant positions}
\usage{
extract_variant_bases(reads, sequences, cigars, variants)
}
\arguments{
\item{reads}{Data.frame. The \verb{$reads} element of a \code{methylation_data} object.
Must contain columns \code{read_name}, \code{start}, \code{end}, and \code{lane}.}

\item{sequences}{Named character vector. The \verb{$sequences} element of a
\code{methylation_data} object, keyed by \code{read_name}.}

\item{cigars}{Named character vector. The \verb{$cigars} element of a
\code{methylation_data} object, keyed by \code{read_name}.}

\item{variants}{Data.frame with columns \code{position}, \code{ref}, \code{alt}, \code{type}
(as returned by \code{\link[=read_variants]{read_variants()}}).}
}
\value{
A data.frame with columns \code{read_name} (character), \code{position}
(integer), \code{base} (character), \code{variant_class} (character), and
\code{lane} (integer).
}
\description{
For each combination of (read, variant position) where the read spans the
variant, walks the CIGAR string to find the query base and classifies it as
\code{"ref"}, \code{"alt"}, \code{"del"} (position falls in a deletion), or \code{"other"}.
}
\keyword{internal}
