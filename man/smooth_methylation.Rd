% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_methylation.R
\name{smooth_methylation}
\alias{smooth_methylation}
\title{Smooth methylation probabilities per group using loess}
\usage{
smooth_methylation(
  sites,
  group_col = "group",
  mod_code_col = NULL,
  span = NULL
)
}
\arguments{
\item{sites}{A data.frame with columns \code{position}, \code{mod_prob}, and a
grouping column (specified by \code{group_col}).}

\item{group_col}{Name of the grouping column (default \code{"group"}).}

\item{span}{Loess smoothing span. When \code{NULL} (default), an adaptive span is
computed per group as \code{max(0.15, min(0.75, 15 / n_unique_sites))}, targeting
approximately 15 data points per local fit regardless of region size or CpG
density. Pass an explicit numeric value (e.g. \code{0.3}) to use a fixed span.}
}
\value{
A data.frame with columns \code{position}, \code{mean_prob}, and the
grouping column. Positions are a regular grid of 200 points with
loess-predicted values. Groups with fewer than 4 unique positions
return raw per-site means instead.
}
\description{
Computes loess-smoothed mean modification probability per group across
genomic positions. Per-site means are calculated first, then a loess curve
is fit and predicted on a regular grid of 200 points spanning the range
of input positions.
}
\keyword{internal}
