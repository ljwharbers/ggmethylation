% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_annotations.R
\name{load_annotations}
\alias{load_annotations}
\title{Load a TxDb annotation source (once, for reuse across regions)}
\usage{
load_annotations(txdb = NULL, gtf = NULL)
}
\arguments{
\item{txdb}{A \code{TxDb} object, or \code{NULL}. Exactly one of \code{txdb} or \code{gtf} must
be provided.}

\item{gtf}{Character. Path to a GTF or GFF annotation file, or \code{NULL}.
When supplied, the entire file is imported with \code{rtracklayer::import()}
and converted to a \code{TxDb} via
\code{GenomicFeatures::makeTxDbFromGRanges()}. For large genomes this can take
tens of seconds; calling this function once and reusing the returned
object is strongly recommended.}
}
\value{
A \code{TxDb} object suitable for repeated calls to \code{\link[=read_annotations]{read_annotations()}}.
}
\description{
Imports a GTF/GFF file or validates a pre-built \code{TxDb} object and returns
the \code{TxDb}. Use this once and pass the result to \code{\link[=read_annotations]{read_annotations()}} for
each region you want to plot, avoiding repeated re-parsing of large GTF
files.
}
\examples{
\dontrun{
# Load once
txdb <- load_annotations(gtf = "Homo_sapiens.GRCh38.gtf")

# Reuse for multiple regions — fast after the first call
ann1 <- read_annotations(txdb = txdb, region = "chr1:1000000-2000000")
ann2 <- read_annotations(txdb = txdb, region = "chr2:5000000-6000000")
}

}
