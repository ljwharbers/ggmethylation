% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.query_extent}
\alias{.query_extent}
\title{Query-space extent of an alignment in original read orientation}
\usage{
.query_extent(cigar, strand)
}
\arguments{
\item{cigar}{Character. A single CIGAR string.}

\item{strand}{Character. \code{"+"} or \code{"-"}.}
}
\value{
A named list with \code{start}, \code{end} (0-based, inclusive, in original
read orientation) and \code{qlen} (full read length), or \code{NULL} when the CIGAR
is missing, unaligned, or has no aligned query bases.
}
\description{
Computes where an alignment sits along the read as it came off the
sequencer, so that a primary alignment and the entries of its \code{SA} tag can
be compared on a common axis.
}
\details{
Hard-clipped bases are counted towards the full read length, which is what
makes the comparison valid: a primary alignment normally soft-clips the
portion aligned elsewhere while its supplementary counterpart hard-clips the
portion aligned here, yet both CIGARs then describe the same total read
length.  A reverse-strand alignment is stored reverse-complemented relative
to the read, so its \emph{trailing} clip is the read-5' offset.
}
\keyword{internal}
