% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_methylation.R
\name{read_methylation}
\alias{read_methylation}
\title{Read methylation data from a modBAM file}
\usage{
read_methylation(
  bam,
  region,
  mod_code = "m",
  group_tag = NULL,
  snv_position = NULL,
  ref_base = NULL,
  alt_base = NULL,
  max_reads = 200L,
  per_group_downsample = FALSE,
  min_mapq = 0L,
  strand_filter = c("+", "-"),
  min_read_length = 0L,
  drop_na_group = FALSE
)
}
\arguments{
\item{bam}{Character. Path to a BAM file containing MM/ML modification tags.}

\item{region}{Character. Genomic region string, e.g. \code{"chr1:1000-2000"}.}

\item{mod_code}{Character. Modification code from the MM tag (default \code{"m"}
for 5-methylcytosine).}

\item{group_tag}{Character or NULL. BAM tag used to group reads (e.g.
\code{"HP"} for haplotype, \code{"RG"} for read group). NULL disables grouping.}

\item{snv_position}{Integer or NULL. Reference position of a heterozygous
SNV to group reads by. Mutually exclusive with \code{group_tag}. When set,
\code{ref_base} and \code{alt_base} are required and reads are labelled \code{"REF"} or
\code{"ALT"} by the base they carry; reads matching neither are dropped.}

\item{ref_base}{Character or NULL. Reference allele at \code{snv_position},
a single base. Required when \code{snv_position} is set.}

\item{alt_base}{Character or NULL. Alternate allele at \code{snv_position},
a single base. Required when \code{snv_position} is set.}

\item{max_reads}{Integer. Maximum number of reads to return (default 200).
If more reads overlap the region, a random subset is kept.}

\item{per_group_downsample}{Logical. When \code{TRUE} and grouping is active
(via \code{group_tag} or \code{snv_position}), the \code{max_reads} cap is applied
independently per group. When \code{FALSE} (default), the existing global
cap behaviour is unchanged.}

\item{min_mapq}{Integer. Minimum mapping quality (MAPQ) threshold
(default \code{0L}, no filtering). Reads with MAPQ below this value,
or with missing MAPQ, are excluded before MM/ML parsing.}

\item{strand_filter}{Character vector. Which strands to include. One or
both of \code{"+"} and \code{"-"} (default \code{c("+", "-")}, both
strands). Use \code{"+"} or \code{"-"} alone to restrict to a single
strand.}

\item{min_read_length}{Integer. Minimum read length in reference-space
base pairs (default \code{0L}, no filtering). Reads shorter than this
value are excluded before MM/ML parsing.}

\item{drop_na_group}{Logical. When \code{TRUE} and \code{group_tag} is set, reads
where the tag is absent (i.e. group is \code{NA}) are removed before
downsampling. Default \code{FALSE} preserves the existing behaviour of keeping
unphased reads in the data.}
}
\value{
A \code{methylation_data} object (S3 list) with elements:
\describe{
\item{reads}{Data.frame with columns \code{read_name}, \code{start}, \code{end},
\code{bam_pos} (original unclipped alignment start, equal to the BAM POS
field), \code{strand}, \code{is_supplementary} (logical), \code{sa_chrom} (chr or
NA), \code{sa_pos} (int or NA), \code{clip_side} (chr: \code{"left"}, \code{"right"},
\code{"both"}, or NA), \code{sa_side} (the reference flank(s) where a
supplementary partner joins this alignment, in read coordinates:
\code{"left"}, \code{"right"}, \code{"both"}, or NA), \code{sa_chrom_left} / \code{sa_pos_left}
and \code{sa_chrom_right} / \code{sa_pos_right} (the partner on each flank, NA
when that flank has none), and optionally \code{group}. \code{start} and \code{end}
are clipped to the queried region; \code{bam_pos} retains the original
start. Note that \code{clip_side} merely reports whether the CIGAR begins
or ends in \code{S}/\code{H} — for long reads that is usually \code{"both"} because
of adapter trimming — whereas \code{sa_side} reflects where a supplementary
alignment actually is.}
\item{sites}{Data.frame with columns \code{position}, \code{mod_prob},
\code{read_name}, \code{mod_code}, and optionally \code{group}.}
\item{region}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} object for the queried region.}
\item{mod_code}{The modification code(s) used.}
\item{group_tag}{The grouping tag used, or \code{NULL}.}
\item{snv_position}{The SNV position used for grouping, or \code{NULL}.}
\item{sequences}{Named character vector of read sequences, keyed by
\code{read_name}. Used internally by \code{\link[=plot_methylation]{plot_methylation()}} for variant
overlay.}
\item{cigars}{Named character vector of CIGAR strings, keyed by
\code{read_name}. Used internally for CIGAR feature decomposition.}
\item{cigar_features}{Data.frame of structural CIGAR features (type,
ref_start, ref_end, query_start, query_end, length, read_name) for
insertions (\code{"I"}), deletions (\code{"D"}), and skipped regions (\code{"N"}).}
\item{insertion_sites}{Data.frame of modification calls that fall on
inserted bases (no reference coordinate). Columns: \code{read_name},
\code{ref_anchor} (reference position the insertion sits before),
\code{query_pos} (1-based position in the read sequence), \code{ins_offset}
(1-based position within the insertion), \code{ins_length}, \code{mod_prob},
\code{mod_code}, and optionally \code{group}. Zero rows when no insertions
carry modification calls.}
}
}
\description{
Parses a BAM file with MM/ML tags and extracts base modification data for
a given genomic region. This is the main entry point for data extraction
(Layer 1 of the package).
}
\examples{
\dontrun{
md <- read_methylation("sample.bam", "chr1:1000-2000")
md <- read_methylation("sample.bam", "chr1:1000-2000",
  group_tag = "HP", max_reads = 100
)
}

}
