% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write_methylation.R
\name{write_methylation}
\alias{write_methylation}
\title{Write methylation data to disk}
\usage{
write_methylation(
  data,
  prefix,
  format = c("tsv", "bed"),
  reads = TRUE,
  sites = TRUE,
  gzip = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{methylation_data} object as returned by \code{\link[=read_methylation]{read_methylation()}}.}

\item{prefix}{Character. Path prefix for output files. The appropriate
suffix (\verb{_reads.tsv}, \verb{_sites.tsv}, or \verb{_sites.bed}) is appended
automatically. Intermediate directories are created if they do not exist.}

\item{format}{Character. Output format for the sites file: \code{"tsv"} (default)
or \code{"bed"} (6-column BED, 0-based coordinates, no header).}

\item{reads}{Logical. Write the \verb{$reads} data frame? Default \code{TRUE}.}

\item{sites}{Logical. Write the \verb{$sites} data frame? Default \code{TRUE}.}

\item{gzip}{Logical. Compress output files with gzip? Default \code{FALSE}.
When \code{TRUE}, \code{.gz} is appended to each file name.}

\item{...}{Reserved for future use; currently ignored.}
}
\value{
\code{data}, invisibly. Allows use in a pipeline.
}
\description{
Saves the \verb{$reads} and/or \verb{$sites} data frames from a \code{methylation_data}
object to tab-separated (TSV) or BED files. Files can optionally be
gzip-compressed.
}
\details{
\strong{Reads file} (\verb{\{prefix\}_reads.tsv[.gz]})

Always written as TSV with a header line. Columns: \code{read_name}, \code{start},
\code{end}, \code{strand}, \code{group} (only when the object carries a group tag),
\code{mean_mod_prob}. \code{mean_mod_prob} is the per-read mean of \code{mod_prob} values
across all sites; reads with no called sites receive \code{NA}.

\strong{Sites file — TSV} (\verb{\{prefix\}_sites.tsv[.gz]})

Columns: \code{position}, \code{mod_prob}, \code{read_name}, \code{mod_code}, \code{group}
(if present). Coordinates are 1-based (as stored in the object).

\strong{Sites file — BED} (\verb{\{prefix\}_sites.bed[.gz]})

6-column BED (no header). Coordinates are converted to 0-based half-open
intervals (\code{chromStart = position - 1}, \code{chromEnd = position}). The \code{score}
column contains \code{mod_prob} (float in \link{0, 1}).
}
\examples{
\dontrun{
md <- read_methylation("sample.bam", "chr1:1000-2000")

# Write both TSV files to the current directory
write_methylation(md, prefix = "output/sample_chr1")

# BED format for sites, gzip-compressed
write_methylation(md, prefix = "output/sample_chr1",
                  format = "bed", gzip = TRUE)

# Reads only
write_methylation(md, prefix = "output/sample_chr1",
                  sites = FALSE)

# Pipe-friendly usage
md |>
  write_methylation(prefix = "output/sample_chr1") |>
  plot_methylation()
}

}
\seealso{
\code{\link[=read_methylation]{read_methylation()}}
}
