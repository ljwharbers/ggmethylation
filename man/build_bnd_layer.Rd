% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variant_overlay_bnd.R
\name{build_bnd_layer}
\alias{build_bnd_layer}
\title{Build ggplot2 layers marking VCF BND positions on the read panel}
\usage{
build_bnd_layer(bnd_df)
}
\arguments{
\item{bnd_df}{Data.frame — BND rows from \code{variant_data$variants} (columns:
\code{position} (int), \code{mate_chrom} (chr), \code{mate_pos} (int)). May be \code{NULL}
or zero-row.}
}
\value{
A flat list with one ggplot2 \code{geom_text} layer per BND showing the
full mate location (chromosome:position with comma thousands separator),
or \code{NULL} when \code{bnd_df} is \code{NULL} or zero-row.
}
\description{
Returns a flat list of ggplot2 layer objects (one \code{geom_text} per BND)
showing the full mate location (chromosome:position with comma thousands
separator) using the join symbol (⋈).
}
\details{
Because the read panel uses \code{scale_y_reverse()}, \code{y = -Inf} places the
text at the \emph{top} of the panel (the intended position).
}
\keyword{internal}
