% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sa_partner_sides}
\alias{sa_partner_sides}
\title{Locate supplementary-alignment partners relative to a primary alignment}
\usage{
sa_partner_sides(cigar, strand, sa_string)
}
\arguments{
\item{cigar}{Character. CIGAR of the primary (this) alignment.}

\item{strand}{Character. Strand of the primary alignment, \code{"+"} or \code{"-"}.}

\item{sa_string}{Character. The raw \code{SA} tag value, or \code{NA}.}
}
\value{
A data.frame with one row per parseable \code{SA} entry and columns
\code{rname}, \code{pos}, \code{mapq}, and \code{side} (\code{"left"}, \code{"right"}, or \code{NA} when the
junction side cannot be determined). Zero rows when there are no entries.
}
\description{
For a chimeric read, each \code{SA} entry joins the primary alignment at exactly
one point in read coordinates.  This helper works out, for every entry,
which \emph{reference} flank of the primary alignment that junction falls on.
}
\details{
The clipped side of a CIGAR is not a usable proxy: long ONT/PacBio reads are
routinely soft-clipped at both ends by adapter and quality trimming, so
\code{\link[=detect_clip_side]{detect_clip_side()}} reports \code{"both"} for reads that have only a single
supplementary partner.  Instead, the entry's position along the read is
compared with the primary's via \code{.query_extent()}: an entry lying downstream
of the primary in read space joins at the primary's read-3' end, which is
its reference right edge on the forward strand and its left edge on the
reverse strand.
}
\keyword{internal}
