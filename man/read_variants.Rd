% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_variants.R
\name{read_variants}
\alias{read_variants}
\title{Read variants from a VCF file}
\usage{
read_variants(vcf, region)
}
\arguments{
\item{vcf}{Character. Path to a bgzipped VCF file (\code{.vcf.gz}) with a
corresponding tabix index (\code{.vcf.gz.tbi}).}

\item{region}{Character. Genomic region string, e.g. \code{"chr1:1000-2000"}.
The same format accepted by \code{\link[=read_methylation]{read_methylation()}}.}
}
\value{
A \code{variant_data} object (S3 list) with elements:
\describe{
\item{variants}{Data.frame with columns \code{position} (integer),
\code{ref} (character), \code{alt} (character), and \code{type} (character:
\code{"SNV"}, \code{"insertion"}, or \code{"deletion"}).}
\item{region}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} object for the queried region.}
}
}
\description{
Loads variant calls from a bgzipped, tabix-indexed VCF file for a given
genomic region. Returns a \code{variant_data} object containing a data.frame of
variants and the queried region as a \code{GRanges} object.
}
\details{
Requires the \code{VariantAnnotation} and \code{SummarizedExperiment} packages from
Bioconductor. Install them with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{BiocManager::install(c("VariantAnnotation", "SummarizedExperiment"))
}\if{html}{\out{</div>}}
}
\examples{
\dontrun{
vd <- read_variants("calls.vcf.gz", "chr1:1000000-1050000")
print(vd)
}

}
