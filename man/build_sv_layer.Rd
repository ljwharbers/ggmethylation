% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variant_overlay_sv.R
\name{build_sv_layer}
\alias{build_sv_layer}
\title{Build ggplot2 layers for SV spans on reads}
\usage{
build_sv_layer(reads, sv_df, region_start, region_end)
}
\arguments{
\item{reads}{Data.frame with columns \code{read_name}, \code{start}, \code{end}, \code{lane}
(the \verb{$reads} element of a \code{methylation_data} object).}

\item{sv_df}{Data.frame with columns \code{position} (int), \code{end} (int), \code{type}
(chr: \code{"DEL"}, \code{"DUP"}, or \code{"INV"}). Only the SV rows from
\code{variant_data$variants}. May be \code{NULL} or zero-row.}

\item{region_start}{Integer. Left boundary of the visible plot window.}

\item{region_end}{Integer. Right boundary of the visible plot window.}
}
\value{
A flat list of ggplot2 layer objects (\code{ggnewscale::new_scale_colour()}
plus \code{geom_segment} and \code{geom_text} layers), or \code{NULL} when there are no
SV–read overlaps to draw.
}
\description{
For each SV in \code{sv_df} (type DEL/DUP/INV), intersects with overlapping reads
and returns a flat list of ggplot2 layer objects (preceded by
\code{ggnewscale::new_scale_colour()}) that draw the SV spans on the read panel.
}
\keyword{internal}
