% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{sa_columns}
\alias{sa_columns}
\title{Build the supplementary-alignment columns for a set of alignments}
\usage{
sa_columns(cigar, strand, sa_tags)
}
\arguments{
\item{cigar}{Character vector of CIGAR strings, one per alignment.}

\item{strand}{Character vector of strands, one per alignment.}

\item{sa_tags}{List or character vector of raw \code{SA} tag values, one per
alignment, or \code{NULL} when the BAM carries no \code{SA} tag at all.}
}
\value{
A data.frame with \code{length(cigar)} rows and columns \code{sa_chrom},
\code{sa_pos}, \code{sa_side}, \code{sa_chrom_left}, \code{sa_pos_left}, \code{sa_chrom_right},
\code{sa_pos_right}. All \code{NA} for alignments with no usable \code{SA} entry.
}
\description{
Vectorised over alignments.  For each one, \code{\link[=sa_partner_sides]{sa_partner_sides()}} decides which
reference flank every \code{SA} partner joins; the best partner (by MAPQ) is then
recorded overall and per flank.
}
\details{
\code{sa_chrom} / \code{sa_pos} are populated from the best partner regardless of
whether its flank could be determined, so BND matching still works for reads
whose junction side is undecidable.
}
\keyword{internal}
