% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{.validate_sort_by}
\alias{.validate_sort_by}
\title{Validate \code{sort_by} against the available read columns}
\usage{
.validate_sort_by(sort_by, reads)
}
\arguments{
\item{sort_by}{Character vector of column names to sort by.}

\item{reads}{The \verb{$reads} data frame the names must exist in.}
}
\value{
\code{sort_by}, invisibly, when every name is valid.
}
\description{
\code{plot_methylation()} sorts reads with \code{order()} over columns pulled out of
\verb{$reads} by name. An unknown name yields \code{NULL}, and \code{order(NULL)} returns
\code{integer(0)} -- which silently drops every read and produces an empty plot
rather than an error. This helper turns that into an explicit failure.
}
\keyword{internal}
