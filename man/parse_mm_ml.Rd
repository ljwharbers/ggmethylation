% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_mm_ml.R
\name{parse_mm_ml}
\alias{parse_mm_ml}
\title{Parse MM and ML tags for a single read}
\usage{
parse_mm_ml(seq, mm_tag, ml_tag, mod_code, strand, cigar, pos)
}
\arguments{
\item{seq}{Character. The read sequence from the BAM SEQ field.}

\item{mm_tag}{Character. The MM tag value, e.g. \code{"C+m,0,1,3;C+h,2;"}.}

\item{ml_tag}{Integer vector. ML tag values (0--255), concatenated for all
modifications listed in the MM tag.}

\item{mod_code}{Character. The modification code to extract (e.g., \code{"m"}
for 5mC, \code{"h"} for 5hmC).}

\item{strand}{Character. \code{"+"} or \code{"-"}, the strand of the alignment.}

\item{cigar}{Character. The CIGAR string for the alignment.}

\item{pos}{Integer. 1-based leftmost alignment position (BAM POS field).}
}
\value{
A named \code{list} with two \code{data.frame} elements:
\describe{
\item{sites}{Reference-aligned modifications. Columns \code{position}
(integer, 1-based genomic position) and \code{mod_prob} (numeric,
ML value / 255). When the MM entry uses the \code{.} implicit-unmodified
flag, also includes rows for unlisted canonical positions with
\code{mod_prob = 0}.}
\item{insertion_sites}{Modifications whose query base falls inside
a CIGAR \code{I} interval (no reference position). Columns \code{query_pos}
(integer, 1-based position in the read sequence) and \code{mod_prob}
(numeric). Modifications inside soft/hard clips are still dropped.
Implicit-zero positions (\code{.} flag) inside insertions are not emitted.}
}
Returns both frames empty (zero rows, same columns) when the
requested modification is not present.
}
\description{
Extracts modification probabilities and their genomic positions from the
MM and ML tags of a single BAM read. Supports the SAM specification for
base modification tags.
}
\keyword{internal}
