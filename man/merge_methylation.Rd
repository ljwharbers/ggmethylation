% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_methylation.R
\name{merge_methylation}
\alias{merge_methylation}
\title{Merge multiple methylation data objects into a multi-sample object}
\usage{
merge_methylation(..., .list = NULL)
}
\arguments{
\item{...}{Named \code{methylation_data} objects. Every argument \strong{must} be
named; an error is raised if any name is missing or empty.}

\item{.list}{A named list of \code{methylation_data} objects. When provided,
\code{...} is ignored. This is useful for programmatic construction of the
sample list.}
}
\value{
A \code{multi_methylation_data} object (S3 list) with elements:
\describe{
\item{samples}{A named list of \code{methylation_data} objects, one per
sample.}
\item{region}{A \link[GenomicRanges:GRanges-class]{GenomicRanges::GRanges} object for the shared
genomic region.}
\item{mod_code}{Character vector of modification code(s) shared by
all samples.}
}
}
\description{
Combines two or more \code{\link[=read_methylation]{methylation_data}} objects
into a single \code{multi_methylation_data} object suitable for multi-sample
plotting with \code{\link[=plot_methylation]{plot_methylation()}}.
}
\details{
All samples must cover the same genomic region and use the same
modification code(s). Every sample must be supplied with a unique name,
either via R's named-argument syntax (\code{merge_methylation(A = md1, B = md2)})
or via the \code{.list} parameter (\code{merge_methylation(.list = list(A = md1, B = md2))}).
}
\examples{
\dontrun{
md1 <- read_methylation("sample1.bam", "chr1:1000-2000")
md2 <- read_methylation("sample2.bam", "chr1:1000-2000")
merged <- merge_methylation(sampleA = md1, sampleB = md2)
print(merged)
plot_methylation(merged)
}

}
