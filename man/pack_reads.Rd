% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pack_reads.R
\name{pack_reads}
\alias{pack_reads}
\title{Pack reads into horizontal lanes for compact visualization}
\usage{
pack_reads(reads, gap = 10)
}
\arguments{
\item{reads}{A data.frame with columns \code{read_name}, \code{start}, and \code{end}.
Rows should already be sorted in the desired display order.}

\item{gap}{Minimum gap in base pairs between reads on the same lane
(default 10).}
}
\value{
An integer vector of lane assignments (same length as
\code{nrow(reads)}), where lane 1 is the top.
}
\description{
Assigns reads to horizontal lanes (rows) using greedy interval scheduling,
similar to how a genome browser packs aligned reads. Each lane contains
non-overlapping reads separated by at least \code{gap} base pairs.
}
\keyword{internal}
